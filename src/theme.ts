import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#ff9800",
    },
    secondary: {
      main: "#ffb74d",
    },
  },
});
export default theme;
