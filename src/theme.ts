import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#157140",
    },
    secondary: {
      main: "#12D46C",
    },
  },
});
export default theme;
